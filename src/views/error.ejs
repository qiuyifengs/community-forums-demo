<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta name="description" content="">
        <meta name="author" content="">
        <title>Free Bootstrap Error Template</title>
        <!-- Bootstrap core CSS -->
        <link href="http://cdn.bootcss.com/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet">
        <!--GOOGLE FONT -->
        <link href='http://fonts.googleapis.com/css?family=Nova+Flat' rel='stylesheet' type='text/css'>
        <!-- custom CSS here -->
        <link href="/style/error/error.css" rel="stylesheet" />
    </head>
    <body>
        <div class="container">
            <div class="row pad-top text-center">
                <div class="col-md-6 col-md-offset-3 text-center">
                    <h1>  What have you done? </h1>
                    <h5> Now Go Back Using Below LInk</h5> 
                    <span id="error-link"></span>
                    <h2>Error: <%= message.message %></h2>
                    <div class="status" style="display: none"><%= message.code %></div>
                </div>
            </div>
                <div class="row text-center">
                <div class="col-md-8 col-md-offset-2">
                    <h3> <img class="light-icon" src="/images/light.png" alt="" srcset=""> </h3>
                    <a href="#" class="btn btn-primary error-btn">GO TO HOME PAGE</a> 
                </div>
            </div>
        </div>
        <script src="/js/jquery-3.3.1.min.js"></script>
        <script src="/js/common.js"></script>
        <script src="/js/bootstrap.js"></script>
        <script src="/js/countUp.js"></script>
        <script type="text/JavaScript">
            (function ($) {
                let status = $(".status").text();
                var mainApp = {
                    main_fun: function () {
                        var count = new countUp("error-link", 10, status, 0, 0.5); //CHANGE 404 TO THE ERROR VALUE AS YOU WANT
                        window.onload = function () {
                            count.start();
                        }
                    },
                    initialization: function () {
                        mainApp.main_fun();
                    }
                }
                $(document).ready(function () {
                    mainApp.main_fun();
                });
                $(".error-btn").on("click", function() {
                    goPage('');
                })
            }(jQuery));
        </script>
    </body>
</html>
